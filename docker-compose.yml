services:
  hurricane-server:
    build:
      context: ./docker
      dockerfile: Dockerfile
    ports: 
      - "7000:8000"
  hurricane-deploy:
    build:
      context: ./hurricane-deploy/docker
      dockerfile: Dockerfile
  hurricane-lab:
    build: 
      context: ./HurricaneDissertation/docker
      dockerfile: Dockerfile
    ports:
      - "6006:6006"
      - "6005:8888"
      
    
